<section>
  <h2>
    All Merchants
  </h2>

  <div class="list_tiles">
    <% @users.each do |user| %>
      <div class="merchant_tile">
        <%= link_to user.username, user_path(user.id) %>
      </div>
    <% end %>
  </div>
</section>